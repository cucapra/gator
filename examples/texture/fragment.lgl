
declare vec4 texture2D(sampler2D txt, vec2 texCoord);

void main(sampler2D uTexture, vec2 vTexCoord) {
  vec4 color = [0.15, 0.4, 0.25, 1.0];

  vec4 gl_FragColor = texture2D(uTexture, vTexCoord);
}