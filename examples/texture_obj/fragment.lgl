tag color is vec4;

declare `t texture2D<`t>(sampler2D<`t> txt, vec2 texCoord);

void main(sampler2D<color> uTexture, vec2 vTexCoord) {
  color gl_FragColor = texture2D<color>(uTexture, vTexCoord);
}