int factor;

void main() {
    bind multiplier to state.current {
        bind 3 to factor {
            result = multiplier([1., 2., 3.]);
            print result;
        }
    }   
}

program int multiplier (int var1) {
    void main() {
        var2 = var1 * factor;
    }
}