tag pos is vec3;
tag color is vec3;
tag tinted is color;

declare t normalize<t extends vec>(t x);

void main() {
    pos vert = [1., 2., 3.];
    color fail = normal<color>(vert);  // Intended use case
}