`t base<`t : int>(`t x) {
    return x;
}

`t anyt<`t>(`t x) {
    return x;
}

`t mt<`t : mat, `u : mat>(`t m1, `t m2, `u m3) {
    `t first = m1 + m2;
    `u second = 3. * m3;
    return first;
}

void main() {
    print 1;
}