tag s is vec<2>; // This is another comment
tag //not a tag
v //still not//;;
is //nope;;;
vec<3> //there we go!//;
;

void main() {
	// This is a comment
	
	// And another one!
	if (true) // Wow, so many comments
	{ //}}}}}}}}}
	skip //interrupted!
	;
	//{{}}{}{{}asldkfjspodpo98fbh09 0q2i3}43567iop;[;+]
	} //else
	else { //;//}
	skip//;
	; }
	// tag another is bad;
	print 5//t //another interruption!;;;;;
	//alright, I'm done
	;
	//now I'm really done
}
