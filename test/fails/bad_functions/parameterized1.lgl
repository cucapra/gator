tag a is vec<3>;
tag p<`t : vec3> is vec<2>;

`t<a> fail<`t : vec<2>>() { // Like, what does this even mean?  You can't parameterize an abstract type
    return [0., 0.];
}

void main() {
    print 1;
}